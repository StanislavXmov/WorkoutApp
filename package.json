{
  "name": "workoutappelectron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "nodemon --exec electron .",
    "package-win": "electron-packager . --overwrite --platform=win32 --arch=ia32 --icon=assets/icon.ico --prune=false --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Workout App\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.1.0",
    "electron-packager": "^15.1.0",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "electron-store": "^6.0.0"
  }
}
